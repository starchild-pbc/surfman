[package]
name = "surfman-ios-device-test"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["staticlib"]

[dependencies]
euclid = "0.22"
glow = "0.17"
surfman = { path = "..", default-features = false }
